{% extends '@App/AuthorArticle/base.html.twig' %}

{% block body %}

    <a class="btn btn-success" href="{{ path('author_article_new') }}">Create</a>
    {% for article in authorArticles.iterator %}
        <div style="border:1px solid black;margin:5px">
            <a href="{{ path('author_article_view', {'url': article.url}) }}">
                {{ article.name }}
            </a>
            ({{ article.publishDate.format('Y.m.d H:i:s') }})
            <a style="float:right" href="{{ path('author_article_edit', {'id': article.id}) }}">
                edit
            </a>
        </div>
    {% endfor %}
    {% for i in range(1, authorArticles.nbPages) %}
        <a href="{{ path('author_article_index', {'page': i}) }}">{{ i }}</a>
    {% endfor %}
    Total: {{ authorArticles.nbResults }}

{% endblock %}